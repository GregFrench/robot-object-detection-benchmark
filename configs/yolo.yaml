model:
  family: yolo
  weights: yolo11n.pt

training:
  data_yaml: configs/coco_tabletop_subset.yaml
  epochs: 20
  imgsz: 640
  batch_size: 8
  device: auto

outputs:
  project: outputs/yolo_runs
  run_name: tabletop_yolo
